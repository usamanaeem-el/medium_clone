type User
  @model
  @auth(rules: [{ allow: public, operations: [create, update, read] }]) {
  id: ID!
  name: String!
  address: String!
  email: String!
  city: String!
  state: String!
  group: GroupsEnum
}

enum GroupsEnum {
  ADMINS
  CAREGIVERS
  PATIENTS
  HEALTHWORKER
}
